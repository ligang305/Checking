<UserControl x:Class="BGUserControl.DiyWaitingPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BGUserControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" Background="White" Height="300" Width="500" HorizontalAlignment="Center">
        <Border x:Name="BackImage" Width="55" Height="56" HorizontalAlignment="Center" VerticalAlignment="Center" 
                Style="{DynamicResource diyRevolveBorder}" Background ="{DynamicResource waitImage}"/>
        <StackPanel Orientation="Horizontal" Background="White" HorizontalAlignment="Center" >
            <Label x:Name="lblStatus" Style="{DynamicResource diyLabel}" Content="{Binding SignalValue,Mode=TwoWay}" Margin="0,100,0,0"/>
            <Label x:Name="lblTime" Style="{DynamicResource diyLabel}"  Margin="0,100,0,0"/>
        </StackPanel>
        
    </StackPanel> 
</UserControl>
